#include "NaluParser.h"
// #include "h264bitstream/h264_stream.h"

#include <iostream>

void NaluParser::ReadNalu(uint8_t* buff, int size) {
    //int nal_start(0), nal_end(0);
    // read some H264 data into buf
    //h264_stream_t* h = h264_new();
    //find_nal_unit(buff, size, &nal_start, &nal_end);
    //read_debug_nal_unit(h, buff + from + nal_start, nal_end - nal_start);
    //h264_free(h);
}